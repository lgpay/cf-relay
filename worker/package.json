{
  "name": "cf-relay",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker 下载链接中转",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node test/api.test.mjs && node test/homepage.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.134.0"
  }
}
